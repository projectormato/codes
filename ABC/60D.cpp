#include <iostream>
#include <sstream>
#include <string>
#include <map>
using namespace std;

int main(){
	int N, W;
	cin >> N >> W;
	int w[N] , v[N]; 
	for (int i = 0; i < N; ++i){
		cin >> w[i] >> v[i];
	}

}